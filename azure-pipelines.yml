# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, and scan

trigger:
- master
- *

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  displayName: Starter Pipeline
  enabled: true
  inputs:
    targetType: 'inline'
    script: |
      echo "My Pipeline"
